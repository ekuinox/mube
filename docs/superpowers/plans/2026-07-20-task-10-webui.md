# TASK-10 WebUI 実装プラン（yew SPA を Pico W から picoserve で HTTP 配信）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ブラウザから施錠/解錠と現在状態を操作・確認できる yew(WASM) SPA を Pico W に埋め込み、picoserve の HTTP(80) で SPA＋JSON API を直接配信する。

**Architecture:** 生 TCP(6000) の行プロトコルを廃止し HTTP 一本化。純ロジック（状態⇄JSON、ルート→目標状態）は host テスト可能な `mube-core::webapi` に置き、firmware は picoserve ルータで埋め込みアセットと JSON API を配信する薄いアダプタに留める。状態の単一ソース（`LOCK_STATE`/`apply_target`/`SERVO_CMD`）とサーボ・GP17 ボタン・二色 LED・WiFi 土台は不変。

**Tech Stack:** Rust(Embassy, thumbv6m) / picoserve(no_std HTTP) / yew + trunk(wasm32-unknown-unknown) / bun(lockctl.ts) / nix devShell。

## Global Constraints

- Rust コード変更後はコミット前に `nix develop -c cargo host-test` を通す。落ちたまま完了扱いにしない。
- この開発機の非対話シェルには `cargo`/`bun`/`trunk` が PATH に無い。Claude が実行する時は各コマンドに `nix develop -c` を前置する。
- 派生物はコミットしない（`.gitignore` 済み）。`crates/webui/dist/` と `crates/webui/target/` も派生物として ignore する。
- firmware のフルビルド（thumbv6m）は未コミットの CYW43 ブロブが要るため、このワークツリーでは `cargo build` が通らない場合がある。firmware タスクの検証はコンパイル可否を主眼に置き、実 HTTP 接続・サーボ動作は実機確認（本プラン外）とする。
- 依存は crates.io 公開版に固定。embassy 系の現行版は firmware Cargo.toml 準拠: embassy-net=0.9 / embassy-executor=0.10 / embassy-time=0.5 / embassy-sync=0.8 / embassy-rp=0.10。
- WiFi 認証・CYW43 ブロブ・サーボキャリブ定数は無変更。実値を会話やコミットに載せない。
- 作業ワークツリー: `.claude/worktrees/task-10-webui`（ブランチ `worktree-task-10-webui`）。全コマンドはこのディレクトリ基準。
- コミットメッセージ末尾に `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` を付ける。

## File Structure

- `crates/mube-core/src/webapi.rs`（新規）: HTTP 用純ロジック（`Action` / `state_json` / `target_for`）。host テスト同梱。
- `crates/mube-core/src/lock.rs`（改修）: `LockState`＋`toggled()` のみに縮小。text プロトコル用 `decide`/`Outcome`/`reply_for` を撤去。
- `crates/mube-core/src/lib.rs`（改修）: `command`/`serve` モジュール撤去、`webapi` 追加、再エクスポート更新。
- `crates/mube-core/src/command.rs`・`crates/mube-core/src/serve.rs`（削除）: text プロトコル専用。
- `crates/mube-core/Cargo.toml`（改修）: 不要になった `embedded-io-async` 依存と `embassy-futures` dev-dep を撤去。
- `crates/webui/`（新規クレート）: `Cargo.toml` / `src/main.rs`（yew app）/ `index.html` / `Trunk.toml`。wasm32 ビルド。
- `Cargo.toml`（ルート・改修）: `crates/webui` を `exclude` に追加（members には入れない）。
- `rust-toolchain.toml`（改修）: `targets` に `wasm32-unknown-unknown` 追加。
- `flake.nix`（改修）: devShell に `trunk` / `wasm-bindgen-cli` / `binaryen` を追加。
- `crates/firmware/Cargo.toml`（改修）: `picoserve` 追加。
- `crates/firmware/build.rs`（改修）: `crates/webui/dist/` の存在チェック（未生成なら明快にエラー）。
- `crates/firmware/src/main.rs`（改修）: TCP-6000 accept ループ撤去、picoserve HTTP ルータへ差し替え、埋め込みアセット `include_bytes!`。
- `crates/firmware/src/http.rs`（新規・任意分割）: picoserve のルータ定義とハンドラ。main.rs を肥大させないため分離。
- `lockctl.ts`・`lockctl.test.ts`（改修）: 生 TCP → fetch。
- `.gitignore`（改修）: `crates/webui/dist/` 等を追加。
- `docs/firmware.md`・`README.md`（改修）: プロトコル節を HTTP に更新。
- `backlog/tasks/task-10-simple-webui.md`: 完了時に Done へ。

---

### Task 1: `mube-core::webapi` — HTTP 用純ロジック（TDD）

**Files:**
- Create: `crates/mube-core/src/webapi.rs`
- Modify: `crates/mube-core/src/lib.rs`

**Interfaces:**
- Consumes: `crate::lock::LockState`（`Locked`/`Unlocked`/`toggled()`）。
- Produces:
  - `pub enum Action { Lock, Unlock, Toggle, Status }`
  - `pub fn state_json(state: LockState) -> &'static str`
  - `pub fn target_for(action: Action, current: LockState) -> Option<LockState>`

- [ ] **Step 1: 失敗するテストを書く**

`crates/mube-core/src/webapi.rs` を新規作成し、実装より先にテストを置く:

```rust
//! HTTP WebUI 用の純ロジック：状態⇄JSON と、ルート→目標状態のマッピング。
//! HTTP のパース/ソケットは firmware(picoserve) 側。ここは host テスト可能な純関数だけ。

use crate::lock::LockState;

/// WebUI から来る操作。HTTP ルートに対応する。
#[derive(Clone, Copy, PartialEq, Eq, Debug)]
pub enum Action {
    Lock,
    Unlock,
    Toggle,
    Status,
}

/// 状態の JSON 表現（API レスポンスボディ）。
pub fn state_json(state: LockState) -> &'static str {
    match state {
        LockState::Locked => "{\"state\":\"LOCKED\"}",
        LockState::Unlocked => "{\"state\":\"UNLOCKED\"}",
    }
}

/// 操作と現在状態から、駆動すべき目標状態を決める。
/// Status は駆動しない（None）。Lock/Unlock は固定、Toggle は現在状態を反転。
pub fn target_for(action: Action, current: LockState) -> Option<LockState> {
    match action {
        Action::Lock => Some(LockState::Locked),
        Action::Unlock => Some(LockState::Unlocked),
        Action::Toggle => Some(current.toggled()),
        Action::Status => None,
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn state_json_maps_both_states() {
        assert_eq!(state_json(LockState::Locked), "{\"state\":\"LOCKED\"}");
        assert_eq!(state_json(LockState::Unlocked), "{\"state\":\"UNLOCKED\"}");
    }

    #[test]
    fn lock_and_unlock_are_fixed_targets() {
        assert_eq!(target_for(Action::Lock, LockState::Unlocked), Some(LockState::Locked));
        assert_eq!(target_for(Action::Lock, LockState::Locked), Some(LockState::Locked));
        assert_eq!(target_for(Action::Unlock, LockState::Locked), Some(LockState::Unlocked));
    }

    #[test]
    fn toggle_flips_current() {
        assert_eq!(target_for(Action::Toggle, LockState::Locked), Some(LockState::Unlocked));
        assert_eq!(target_for(Action::Toggle, LockState::Unlocked), Some(LockState::Locked));
    }

    #[test]
    fn status_does_not_drive() {
        assert_eq!(target_for(Action::Status, LockState::Locked), None);
        assert_eq!(target_for(Action::Status, LockState::Unlocked), None);
    }
}
```

`crates/mube-core/src/lib.rs` に `pub mod webapi;` を追加（この時点では他モジュールはまだ残っていてよい。撤去は Task 2）:

```rust
pub mod webapi;
```

- [ ] **Step 2: テストが失敗する（＝コンパイルは通り新規テストが認識される）ことを確認**

Run: `nix develop -c cargo host-test webapi`
Expected: `webapi::tests::*` の4テストが走って PASS。
（本タスクの実装はテストと同ファイルに既に書いているため、Step 2 で PASS する。純関数のためこの構成を採用。もし赤にしたければ関数本体を `todo!()` にして FAIL を観測してから戻す。）

- [ ] **Step 3: `lib.rs` の再エクスポートを追加**

`crates/mube-core/src/lib.rs` の `pub use` に追記:

```rust
pub use webapi::{state_json, target_for, Action};
```

- [ ] **Step 4: 全 host テストが緑であることを確認**

Run: `nix develop -c cargo host-test`
Expected: 既存テスト＋`webapi` の4テストが全て PASS。

- [ ] **Step 5: コミット**

```bash
git add crates/mube-core/src/webapi.rs crates/mube-core/src/lib.rs
git commit -m "feat(core): HTTP WebUI 用純ロジック webapi（Action/state_json/target_for）を追加

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: text プロトコル資産の撤去（serve.rs / command.rs / decide）

**Files:**
- Delete: `crates/mube-core/src/serve.rs`, `crates/mube-core/src/command.rs`
- Modify: `crates/mube-core/src/lock.rs`, `crates/mube-core/src/lib.rs`, `crates/mube-core/Cargo.toml`

**Interfaces:**
- Produces: `mube-core` の公開 API は `LockState` / `webapi::{Action, state_json, target_for}` / `servo_math::*` のみになる。`decide`/`Outcome`/`LockPort`/`serve_connection`/`LINE_MAX`/`command::*` は消える。firmware(Task 5) はこの縮小後 API 前提で書く。

- [ ] **Step 1: `lock.rs` を LockState＋toggled に縮小**

`crates/mube-core/src/lock.rs` を丸ごと以下に置き換える（`decide`/`Outcome`/`reply_for` と `command` への依存、text プロトコル用テストを削除。`toggled` とそのテストは残す）:

```rust
//! ロック状態。施錠/解錠の 2 状態と反転操作。

/// 施錠/解錠の 2 状態。
#[derive(Clone, Copy, PartialEq, Eq, Debug)]
#[cfg_attr(feature = "defmt", derive(defmt::Format))]
pub enum LockState {
    Locked,
    Unlocked,
}

impl LockState {
    /// 施錠⇄解錠を反転する。ボタンのトグル操作・toggle API で使う。
    pub fn toggled(self) -> LockState {
        match self {
            LockState::Locked => LockState::Unlocked,
            LockState::Unlocked => LockState::Locked,
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn toggled_flips_state() {
        assert_eq!(LockState::Locked.toggled(), LockState::Unlocked);
        assert_eq!(LockState::Unlocked.toggled(), LockState::Locked);
    }
}
```

- [ ] **Step 2: text プロトコルのモジュールを削除**

```bash
git rm crates/mube-core/src/serve.rs crates/mube-core/src/command.rs
```

- [ ] **Step 3: `lib.rs` を更新**

`crates/mube-core/src/lib.rs` を以下に置き換える:

```rust
//! ハード非依存のロック制御ロジック。host で cargo test できる
//! （firmware からは no_std 依存として使う）。
#![cfg_attr(not(test), no_std)]

pub mod lock;
pub mod servo_math;
pub mod webapi;

pub use lock::LockState;
pub use webapi::{state_json, target_for, Action};
```

- [ ] **Step 4: `Cargo.toml` から不要依存を撤去**

`crates/mube-core/Cargo.toml` の `[dependencies]` から `embedded-io-async`（serve.rs 専用だった）、`[dev-dependencies]` から `embassy-futures`（serve テストの block_on 専用だった）を削除する。結果:

```toml
[dependencies]
defmt = { version = "0.3", optional = true }

[features]
defmt = ["dep:defmt"]
```

- [ ] **Step 5: host テストが緑であることを確認**

Run: `nix develop -c cargo host-test`
Expected: `lock::tests::toggled_flips_state`・`webapi::tests::*`・`servo_math::tests::*` が PASS。`serve`/`command` のテストは消滅。0 failed。

- [ ] **Step 6: コミット**

```bash
git add -A crates/mube-core
git commit -m "refactor(core): 生 TCP text プロトコル資産（serve/command/decide）を撤去し HTTP 一本化へ

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: `crates/webui` — yew SPA とビルドツールチェーン

**Files:**
- Create: `crates/webui/Cargo.toml`, `crates/webui/src/main.rs`, `crates/webui/index.html`, `crates/webui/Trunk.toml`
- Modify: `Cargo.toml`(ルート・workspace exclude), `rust-toolchain.toml`, `flake.nix`, `.gitignore`

**Interfaces:**
- Produces: `nix develop -c trunk build` が `crates/webui/dist/` に固定名 `index.html` / `webui.js` / `webui_bg.wasm` を出力する。firmware(Task 5) はこの3ファイルを `include_bytes!` する。SPA は同一オリジンの `GET /api/status` / `POST /api/lock`|`/api/unlock`|`/api/toggle` を呼ぶ（レスポンス `{"state":"LOCKED"|"UNLOCKED"}`）。

- [ ] **Step 1: ワークスペースから webui を除外**

ルート `Cargo.toml` に `exclude` を追加（wasm クレートを thumbv6m 既定のワークスペースビルドに巻き込まないため。`members` には入れない）:

```toml
[workspace]
resolver = "2"
members = ["crates/firmware", "crates/mube-core"]
exclude = ["crates/webui"]
```

- [ ] **Step 2: toolchain に wasm32 ターゲットを追加**

`rust-toolchain.toml` の `targets` に追記:

```toml
targets = ["thumbv6m-none-eabi", "wasm32-unknown-unknown"]
```

- [ ] **Step 3: devShell に trunk 系を追加**

`flake.nix` の `packages = [ ... ]` に以下3行を追加（`pkgs.bun` の近くに）:

```nix
            pkgs.trunk            # yew(WASM) を dist へビルド（crates/webui）
            pkgs.wasm-bindgen-cli # trunk が使う wasm-bindgen（ネット取得を避け系から供給）
            pkgs.binaryen         # wasm-opt（release 最適化）
```

- [ ] **Step 4: yew クレートを作成**

`crates/webui/Cargo.toml`:

```toml
[package]
name = "mube-webui"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Browser WebUI (yew/WASM) for the mube smart lock"

[dependencies]
yew = { version = "0.21", features = ["csr"] }
gloo-net = "0.6"
wasm-bindgen-futures = "0.4"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
```

`crates/webui/src/main.rs`（現在状態表示＋施錠/解錠ボタン。読込時に status 取得、操作後に応答で更新）:

```rust
//! mube スマートロックのブラウザ WebUI（yew/WASM）。
//! 同一オリジンの JSON API を叩く：GET /api/status, POST /api/lock|unlock。
//! レスポンスは {"state":"LOCKED"|"UNLOCKED"}。

use gloo_net::http::Request;
use wasm_bindgen_futures::spawn_local;
use yew::prelude::*;

#[derive(Clone, Copy, PartialEq)]
enum State {
    Locked,
    Unlocked,
    Unknown,
}

fn parse_state(body: &str) -> State {
    if body.contains("LOCKED") && !body.contains("UNLOCKED") {
        State::Locked
    } else if body.contains("UNLOCKED") {
        State::Unlocked
    } else {
        State::Unknown
    }
}

#[function_component(App)]
fn app() -> Html {
    let state = use_state(|| State::Unknown);

    // 初回マウントで現在状態を取得。
    {
        let state = state.clone();
        use_effect_with((), move |_| {
            let state = state.clone();
            spawn_local(async move {
                if let Ok(resp) = Request::get("/api/status").send().await {
                    if let Ok(text) = resp.text().await {
                        state.set(parse_state(&text));
                    }
                }
            });
            || ()
        });
    }

    // POST してレスポンスの状態で更新するコールバックを作る。
    let post = {
        let state = state.clone();
        Callback::from(move |path: &'static str| {
            let state = state.clone();
            spawn_local(async move {
                if let Ok(resp) = Request::post(path).send().await {
                    if let Ok(text) = resp.text().await {
                        state.set(parse_state(&text));
                    }
                }
            });
        })
    };

    let (label, color) = match *state {
        State::Locked => ("施錠 (LOCKED)", "#c0392b"),
        State::Unlocked => ("解錠 (UNLOCKED)", "#27ae60"),
        State::Unknown => ("― 取得中 ―", "#888"),
    };

    let on_lock = {
        let post = post.clone();
        Callback::from(move |_| post.emit("/api/lock"))
    };
    let on_unlock = {
        let post = post.clone();
        Callback::from(move |_| post.emit("/api/unlock"))
    };

    html! {
        <main style="font-family:sans-serif;max-width:20rem;margin:2rem auto;text-align:center">
            <h1>{ "mube smart lock" }</h1>
            <p style={format!("font-size:1.5rem;font-weight:bold;color:{color}")}>{ label }</p>
            <button style="font-size:1.2rem;padding:0.6rem 1.4rem;margin:0.3rem" onclick={on_lock}>{ "施錠" }</button>
            <button style="font-size:1.2rem;padding:0.6rem 1.4rem;margin:0.3rem" onclick={on_unlock}>{ "解錠" }</button>
        </main>
    }
}

fn main() {
    yew::Renderer::<App>::new().render();
}
```

`crates/webui/index.html`（trunk のエントリ。固定名出力のため下記 Trunk.toml と併用）:

```html
<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>mube smart lock</title>
    <link data-trunk rel="rust" data-bin="webui" />
  </head>
  <body></body>
</html>
```

`crates/webui/Trunk.toml`（固定ファイル名で出力＝firmware から静的パスで include するため）:

```toml
[build]
dist = "dist"
filehash = false
```

- [ ] **Step 5: trunk ビルドが通ることを確認**

Run: `cd crates/webui && nix develop -c trunk build`
Expected: `crates/webui/dist/index.html` / `dist/webui.js` / `dist/webui_bg.wasm` が生成される。
確認: `ls crates/webui/dist/` に3ファイル。`index.html` 内の参照が `webui.js` / `webui_bg.wasm`（ハッシュ無し）になっていること。
※ もし trunk が wasm-bindgen のバージョン不一致でネット取得を試みて失敗する場合: `crates/webui/Cargo.toml` の依存が要求する wasm-bindgen 版と devShell の `pkgs.wasm-bindgen-cli` 版を揃える（`nix develop -c wasm-bindgen --version` で確認し、必要なら `yew`/`gloo-net` の版を、系の wasm-bindgen に合う組合せへ調整）。この整合が本タスクの主なリスク。

- [ ] **Step 6: dist を gitignore**

`.gitignore` に追記:

```
# yew(WASM) WebUI の派生物
/crates/webui/dist/
/crates/webui/target/
```

- [ ] **Step 7: コミット**（`dist/` はコミットしない）

```bash
git add crates/webui/Cargo.toml crates/webui/src/main.rs crates/webui/index.html crates/webui/Trunk.toml Cargo.toml rust-toolchain.toml flake.nix .gitignore
git commit -m "feat(webui): yew SPA と trunk/wasm32 ツールチェーンを追加（2ボタン＋状態表示）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: firmware に picoserve を導入（互換スパイク）

embassy-net 0.9 と互換な picoserve を確定し、最小の1ルートで thumbv6m コンパイルが通ることを先に確認する。ここで picoserve の実 API シグネチャ（Router 構築・レスポンス生成・listen ループ）を確定し、Task 5 はそれに従う。**この分離の狙いは、依存互換という最大リスクを全配線の前に潰すこと。**

**Files:**
- Modify: `crates/firmware/Cargo.toml`
- Create: `crates/firmware/src/http.rs`（最小スケルトン）
- Modify: `crates/firmware/src/main.rs`（モジュール宣言のみ、この時点では serve ループは残置可）

**Interfaces:**
- Produces: 確定した picoserve バージョンと、そのバージョンでの「バイト列＋Content-Type を返すハンドラ」「Router へのルート登録」「TcpSocket での listen」の呼び出し形。Task 5 で使う。

- [ ] **Step 1: picoserve を追加（版は互換確認して固定）**

`crates/firmware/Cargo.toml` の `[dependencies]` に追加する。**embassy-net=0.9 と整合する picoserve の最新版を crates.io で確認して固定する**（picoserve は embassy-net を再エクスポート依存に持つため、版がずれると `TcpSocket` の型不一致になる）。目安として:

```toml
picoserve = { version = "<embassy-net 0.9 と整合する版>", features = ["embassy"] }
```

確認手順: `nix develop -c cargo add picoserve --features embassy --dry-run` で解決される embassy-net 版を見る。解決結果が 0.9 と一致しない場合は、`cargo tree -i embassy-net` で二重取り込みが無いことを確認できる版を選ぶ。どうしても整合版が無い場合は本タスクを中断し、フォールバック（手書き最小 HTTP。設計 §1 の代替案）へ切り替える判断を仰ぐ。

- [ ] **Step 2: 最小ルートのスケルトンを書く**

`crates/firmware/src/http.rs`（picoserve の実 API に合わせて確定する。下記は目標の形。picoserve の該当バージョンの examples/docs でルータ構築とレスポンス生成の正確なシグネチャを確認して合わせる）:

```rust
//! picoserve による HTTP ルータ。埋め込み SPA アセットと JSON API を配信する。
//! ハンドラは firmware の共有状態（LOCK_STATE / apply_target）を直接叩く。

use picoserve::routing::get;

/// スパイク用の最小ルータ（Task 5 で本実装に差し替え）。
pub fn make_app() -> picoserve::Router<impl picoserve::routing::PathRouter> {
    picoserve::Router::new().route("/", get(|| async { "ok" }))
}
```

`crates/firmware/src/main.rs` にモジュール宣言を追加（既存の `mod config; mod servo;` の並び）:

```rust
mod http;
```

- [ ] **Step 3: thumbv6m でコンパイルが通ることを確認**

Run: `nix develop -c cargo build -p mube-firmware`
Expected: コンパイル成功（`http::make_app` が未使用でも警告どまりで緑）。picoserve と embassy-net が単一版で解決されること。
※ CYW43 ブロブ未取得でこのワークツリーの firmware ビルドが別要因で止まる場合は、`nix develop -c cargo check -p mube-firmware` で型解決だけ確認し、その旨を記録する。

- [ ] **Step 4: 確定した picoserve API を記録**

`crates/firmware/src/http.rs` 冒頭コメントに、確定した picoserve バージョンと「レスポンス生成（バイト列＋Content-Type）」「listen ループ」の正確な呼び出し形を1〜2行でメモする（Task 5 実装者への引き継ぎ）。

- [ ] **Step 5: コミット**

```bash
git add crates/firmware/Cargo.toml crates/firmware/src/http.rs crates/firmware/src/main.rs Cargo.lock
git commit -m "chore(firmware): picoserve を導入し embassy-net 0.9 互換を確認（最小ルート緑）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: firmware HTTP ルータ本実装＋埋め込み＋TCP-6000 撤去

**Files:**
- Modify: `crates/firmware/src/http.rs`, `crates/firmware/src/main.rs`, `crates/firmware/build.rs`

**Interfaces:**
- Consumes: `mube_core::{state_json, target_for, Action, LockState}`（Task 1/2）、picoserve API（Task 4 で確定）、`crates/webui/dist/*`（Task 3）。
- Produces: port 80 の HTTP サーバ。ルート `/`・`/webui.js`・`/webui_bg.wasm`・`/api/status`・`/api/lock`・`/api/unlock`・`/api/toggle`。

- [ ] **Step 1: build.rs に dist 存在チェックを追加**

`crates/firmware/build.rs` の末尾（memory.x コピーの後）に、埋め込み対象が無い場合に明快に失敗させる処理を追加:

```rust
    // WebUI の埋め込みアセット（yew/trunk 出力）が無ければ、束ねビルド未実行として明示的に失敗させる。
    let dist = std::path::Path::new("../webui/dist");
    for f in ["index.html", "webui.js", "webui_bg.wasm"] {
        if !dist.join(f).exists() {
            panic!(
                "crates/webui/dist/{f} が無い。先に `cd crates/webui && trunk build` を実行してから firmware をビルドすること。"
            );
        }
    }
    println!("cargo:rerun-if-changed=../webui/dist/index.html");
    println!("cargo:rerun-if-changed=../webui/dist/webui.js");
    println!("cargo:rerun-if-changed=../webui/dist/webui_bg.wasm");
```

- [ ] **Step 2: http.rs にルータ本体を実装**

`crates/firmware/src/http.rs` を本実装に差し替える。埋め込みアセットと JSON API を配信し、ハンドラは既存の共有状態（`LOCK_STATE` の読み取りと `apply_target`）を叩く。API ハンドラは `mube_core::{Action, target_for, state_json}` を使う。picoserve のレスポンス生成 API は Task 4 で確定した形に合わせる（下記はロジックの意図。バイト列＋Content-Type＋ステータスを返す）:

```rust
//! picoserve による HTTP ルータ。埋め込み SPA アセットと JSON API を配信する。
//! 確定 picoserve 版/レスポンス API: <Task 4 のメモを反映>。

use mube_core::{state_json, target_for, Action, LockState};
use picoserve::routing::{get, post};

// yew/trunk の出力を埋め込む（build.rs が存在を保証）。
const INDEX_HTML: &[u8] = include_bytes!("../../webui/dist/index.html");
const WEBUI_JS: &[u8] = include_bytes!("../../webui/dist/webui.js");
const WEBUI_WASM: &[u8] = include_bytes!("../../webui/dist/webui_bg.wasm");

// 共有状態への口。main.rs が実体（LOCK_STATE 参照・apply_target）を渡す。
// picoserve の状態注入を使わず、これらは main.rs 側の関数ポインタ/クロージャで束ねる。
fn current() -> LockState {
    crate::current_state()
}
fn drive(action: Action) -> &'static str {
    let cur = current();
    if let Some(target) = target_for(action, cur) {
        crate::apply_target(target);
        state_json(target)
    } else {
        state_json(cur)
    }
}

/// ルータを構築する。各ハンドラは埋め込みアセットか JSON を返す。
/// レスポンス生成（バイト列＋Content-Type）は picoserve の該当 API に合わせる。
pub fn make_app() -> picoserve::Router<impl picoserve::routing::PathRouter> {
    picoserve::Router::new()
        .route("/", get(|| async { asset(INDEX_HTML, "text/html; charset=utf-8") }))
        .route("/webui.js", get(|| async { asset(WEBUI_JS, "application/javascript") }))
        .route("/webui_bg.wasm", get(|| async { asset(WEBUI_WASM, "application/wasm") }))
        .route("/api/status", get(|| async { json(state_json(current())) }))
        .route("/api/lock", post(|| async { json(drive(Action::Lock)) }))
        .route("/api/unlock", post(|| async { json(drive(Action::Unlock)) }))
        .route("/api/toggle", post(|| async { json(drive(Action::Toggle)) }))
}

// asset()/json(): picoserve のレスポンス型を返すヘルパ。Task 4 で確定した
// レスポンス生成 API（例: picoserve::response::Response とヘッダ付与）で実装する。
```

> 注: `asset`/`json` の具体実装は Task 4 で確定した picoserve のレスポンス生成 API に依存する。バイト列本文＋`Content-Type` ヘッダ＋200 を返す最小形で実装する。`&'static [u8]` はフラッシュ上に置かれ、picoserve がそこからソケットへ流す（RAM に丸ごと載せない）。

- [ ] **Step 3: main.rs を差し替え（共有状態の口＋picoserve 起動、TCP-6000 撤去）**

`crates/firmware/src/main.rs` を次のように改修する:

(a) import を更新（serve/LockPort を除去）:

```rust
use mube_core::LockState;
```
（`use mube_core::serve::serve_connection;` と `use mube_core::{LockPort, LockState};` を削除。`serve_connection`/`FwLockPort` は不要。）

(b) `FwLockPort` 構造体と `impl LockPort for FwLockPort { .. }` を丸ごと削除。`apply_target` と `LOCK_STATE`・`SERVO_CMD` は残す。http.rs から呼べるよう、現在状態取得の薄い関数を追加:

```rust
/// http.rs から現在状態を読むための口。
pub fn current_state() -> LockState {
    LOCK_STATE.lock(|c| c.get())
}
```
（`apply_target` は既に `pub` でなければ `pub(crate)` にして http.rs から呼べるようにする。）

(c) 末尾の TCP accept/serve ループ（`const LOCK_PORT`・`rx_buf`/`tx_buf`・`let port = FwLockPort;`・`loop { TcpSocket::new ... serve_connection ... }`）を削除し、picoserve 起動に差し替える。picoserve は worker を数本 spawn して port 80 を捌く（Task 4 で確定した listen API に合わせる）。目標の形:

```rust
    // HTTP WebUI: port 80 で SPA＋JSON API を配信する。
    const HTTP_PORT: u16 = 80;
    static APP: StaticCell<picoserve::Router<_>> = StaticCell::new();
    let app = APP.init(http::make_app());
    static CONFIG: StaticCell<picoserve::Config<Duration>> = StaticCell::new();
    let config = CONFIG.init(
        picoserve::Config::new(picoserve::Timeouts {
            start_read_request: Some(Duration::from_secs(IDLE_TIMEOUT_SECS)),
            read_request: Some(Duration::from_secs(IDLE_TIMEOUT_SECS)),
            write: Some(Duration::from_secs(IDLE_TIMEOUT_SECS)),
        }),
    );
    // worker を数本（例: 2〜3）spawn。各 worker が自前の TcpSocket＋バッファで port 80 を accept する。
    // 具体の spawn ループ／ソケットバッファは Task 4 で確定した picoserve の listen API に合わせる。
```

> picoserve の実際の起動 API（`picoserve::listen_and_serve` 相当、worker タスク定義、ソケットバッファの受け渡し）は該当バージョンの examples に厳密に合わせる。バッファは 512B 前後・worker 2〜3 本から始め、RAM 256K を圧迫しない範囲に留める。オンボード LED の接続インジケータ点灯は picoserve の接続コールバック有無に依存するため、無ければ常時消灯でよい（状態表示は二色 LED が担うので必須でない）。

- [ ] **Step 4: thumbv6m でコンパイルが通ることを確認**

まず埋め込み対象を用意してから firmware をビルドする:

Run: `cd crates/webui && nix develop -c trunk build && cd ../.. && nix develop -c cargo build -p mube-firmware`
Expected: build.rs の dist チェックを通過し、firmware がコンパイル成功。
※ CYW43 ブロブ未取得でリンク/ビルドが別要因で止まる場合は `nix develop -c cargo check -p mube-firmware` で型解決の緑を確認し、実機ビルド確認は本プラン外（実機フロー）とする旨を記録。

- [ ] **Step 5: host テスト回帰確認**

Run: `nix develop -c cargo host-test`
Expected: mube-core のテストが全緑（firmware 改修が core を壊していないこと）。

- [ ] **Step 6: コミット**

```bash
git add crates/firmware/src/http.rs crates/firmware/src/main.rs crates/firmware/build.rs
git commit -m "feat(firmware): picoserve で yew SPA＋JSON API を port 80 配信、TCP-6000 撤去

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: `lockctl.ts` を fetch(HTTP) へ移行（TDD）

**Files:**
- Modify: `lockctl.ts`, `lockctl.test.ts`

**Interfaces:**
- Consumes: firmware の HTTP API（`GET /api/status`, `POST /api/lock`|`/api/unlock`|`/api/toggle`、応答 `{"state":"LOCKED"|"UNLOCKED"}`）。
- Produces: 同じサブコマンド体験（`lock`/`unlock`/`status`/`toggle`）。純ヘルパ `parseState(json)` / `formatReply(state)` をエクスポート。

- [ ] **Step 1: テストを HTTP モックへ書き換え（失敗させる）**

`lockctl.test.ts` を以下に置き換える（生 TCP モックを HTTP モックに、`LineBuffer`/`toggleTarget` のテストを `parseState` に差し替え）:

```ts
import { test, expect } from "bun:test";
import { parseState, formatReply, runLockctl } from "./lockctl.ts";

test("parseState: JSON から状態を取り出す", () => {
  expect(parseState('{"state":"LOCKED"}')).toBe("LOCKED");
  expect(parseState('{"state":"UNLOCKED"}')).toBe("UNLOCKED");
});

test("parseState: 想定外は null", () => {
  expect(parseState('{"state":"HUH"}')).toBeNull();
  expect(parseState("not json")).toBeNull();
});

test("formatReply: 応答を人間向け表示に変換する", () => {
  expect(formatReply("LOCKED")).toBe("施錠 (LOCKED) / 赤");
  expect(formatReply("UNLOCKED")).toBe("解錠 (UNLOCKED) / 緑");
  expect(formatReply("HUH")).toBeNull();
});

// firmware の HTTP API を模したモックサーバ。
function startMock(initialLocked: boolean) {
  const state = { locked: initialLocked };
  const body = () => JSON.stringify({ state: state.locked ? "LOCKED" : "UNLOCKED" });
  const server = Bun.serve({
    port: 0,
    fetch(req) {
      const url = new URL(req.url);
      if (req.method === "POST" && url.pathname === "/api/lock") state.locked = true;
      else if (req.method === "POST" && url.pathname === "/api/unlock") state.locked = false;
      else if (req.method === "POST" && url.pathname === "/api/toggle") state.locked = !state.locked;
      // GET /api/status は状態を変えない。
      return new Response(body(), { headers: { "content-type": "application/json" } });
    },
  });
  return { server, state };
}

test("runLockctl: モック HTTP 相手に status / toggle / lock が通る", async () => {
  const m = startMock(true);
  const base = `http://127.0.0.1:${m.server.port}`;
  expect(await runLockctl("status", base)).toBe("施錠 (LOCKED) / 赤");
  expect(await runLockctl("toggle", base)).toBe("解錠 (UNLOCKED) / 緑");
  expect(m.state.locked).toBe(false);
  expect(await runLockctl("lock", base)).toBe("施錠 (LOCKED) / 赤");
  expect(m.state.locked).toBe(true);
  m.server.stop();
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `nix develop -c bun test lockctl.test.ts`
Expected: FAIL（`parseState` 未定義、`runLockctl` のシグネチャ不一致）。

- [ ] **Step 3: lockctl.ts を fetch 実装へ書き換える**

`lockctl.ts` を以下に置き換える:

```ts
#!/usr/bin/env bun
// スマートロックの施錠/解錠を HTTP で操作するクライアント。
//   赤 = 施錠 (LOCKED) / 緑 = 解錠 (UNLOCKED)
//
// 接続先は環境変数 TARGET_IP（.envrc.local で定義 → direnv がロード）。ポートは 80 既定（PORT env で上書き可）。
//
// 使い方:
//   bun lockctl.ts            # トグル（引数なし）
//   bun lockctl.ts toggle     # 同上
//   bun lockctl.ts lock       # 施錠（赤）
//   bun lockctl.ts unlock     # 解錠（緑）
//   bun lockctl.ts status     # 現在状態を問い合わせ（駆動しない）

/** {"state":"LOCKED"|"UNLOCKED"} から状態文字列を取り出す。想定外は null。 */
export function parseState(body: string): "LOCKED" | "UNLOCKED" | null {
  try {
    const s = JSON.parse(body)?.state;
    return s === "LOCKED" || s === "UNLOCKED" ? s : null;
  } catch {
    return null;
  }
}

/** 状態を人間向けの表示にする。想定外は null。 */
export function formatReply(state: string): string | null {
  if (state === "LOCKED") return "施錠 (LOCKED) / 赤";
  if (state === "UNLOCKED") return "解錠 (UNLOCKED) / 緑";
  return null;
}

/** コマンドを 1 つ実行して人間向けの結果メッセージを返す。異常は Error を投げる。 */
export async function runLockctl(
  cmd: "toggle" | "lock" | "unlock" | "status",
  base: string,
  timeoutMs = 5000,
): Promise<string> {
  const route =
    cmd === "status" ? "/api/status" : cmd === "toggle" ? "/api/toggle" : `/api/${cmd}`;
  const method = cmd === "status" ? "GET" : "POST";
  let resp: Response;
  try {
    resp = await fetch(base + route, { method, signal: AbortSignal.timeout(timeoutMs) });
  } catch {
    throw new Error(`${base} に接続できない。IP・電源・WiFi 接続を確認してね`);
  }
  if (!resp.ok) throw new Error(`HTTP ${resp.status}`);
  const state = parseState(await resp.text());
  if (state === null) throw new Error("応答の JSON が想定外");
  const msg = formatReply(state);
  if (msg === null) throw new Error(`応答: '${state}'`);
  return msg;
}

const USAGE = "usage: bun lockctl.ts [toggle|lock|unlock|status]";

if (import.meta.main) {
  const cmd = process.argv[2] ?? "toggle";
  if (cmd === "-h" || cmd === "--help" || cmd === "help") {
    console.log(USAGE);
    console.log("  toggle  現在と逆に切り替え（引数なしと同じ）");
    console.log("  lock    施錠（赤）");
    console.log("  unlock  解錠（緑）");
    console.log("  status  現在状態を問い合わせ（駆動しない）");
    process.exit(0);
  }
  if (cmd !== "toggle" && cmd !== "lock" && cmd !== "unlock" && cmd !== "status") {
    console.error(USAGE);
    process.exit(2);
  }
  const host = process.env.TARGET_IP;
  if (!host) {
    console.error("error: TARGET_IP が未設定。.envrc.local を定義して 'direnv allow' したか確認してね");
    process.exit(1);
  }
  const port = process.env.PORT ?? "80";
  const base = `http://${host}:${port}`;
  const timeoutMs = Number(process.env.CONNECT_TIMEOUT ?? "5") * 1000;
  try {
    console.log(await runLockctl(cmd, base, timeoutMs));
  } catch (err) {
    console.error(`error: ${err instanceof Error ? err.message : err}`);
    process.exit(1);
  }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `nix develop -c bun test lockctl.test.ts`
Expected: 全テスト PASS。

- [ ] **Step 5: コミット**

```bash
git add lockctl.ts lockctl.test.ts
git commit -m "feat(lockctl): 生 TCP から HTTP(fetch) へ移行、サブコマンドは据え置き

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: ドキュメント更新とタスククローズ

**Files:**
- Modify: `docs/firmware.md`, `README.md`
- Modify: `backlog/tasks/task-10-simple-webui.md`（Done へ）

**Interfaces:**
- Consumes: 実装済みの HTTP API とビルド手順。

- [ ] **Step 1: docs/firmware.md の「遠隔操作」節を HTTP へ更新**

`docs/firmware.md` の「## 遠隔操作（TCP）」節を書き換える。要点:
- Pico W は WiFi 接続後、**HTTP ポート 80** で WebUI（yew SPA）と JSON API を配信する。
- ブラウザで `http://<pico-ip>/` を開くと状態表示＋施錠/解錠ボタンが使える。
- API: `GET /api/status`→`{"state":"LOCKED"|"UNLOCKED"}`、`POST /api/lock`|`/api/unlock`|`/api/toggle`。
- GP17 物理ボタン・二色 LED は不変で、状態は WebUI と一致。
- `lockctl.ts` は HTTP に移行（`TARGET_IP`、ポート 80 既定・`PORT` で上書き）。使用例は据え置き。
- WebUI は firmware に埋め込むため、firmware ビルド前に `cd crates/webui && trunk build` が必要（未実行なら build.rs が明示エラー）。
- 平文 HTTP・無認証・LAN 内前提（公開網に晒さない）。

- [ ] **Step 2: README.md のコマンド表を更新**

`README.md` のコマンド表に WebUI ビルド行を追加し、プロトコル記述の TCP→HTTP を反映:
- 「WebUI ビルド（yew→dist）」= `cd crates/webui && trunk build`
- 「ファームビルド」行の注に「先に WebUI を trunk build」を追記。
- `lockctl.ts` のポート既定を 80 に。

- [ ] **Step 3: ドキュメントのコミット**

```bash
git add docs/firmware.md README.md
git commit -m "docs: 遠隔操作を HTTP(WebUI) に更新、WebUI ビルド手順を追記

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 4: backlog タスクを Done に**

Run: `nix develop -c backlog task edit 10 -s Done`（AC #1/#2 も満たす旨をタスクに反映）
Expected: `task-10-simple-webui.md` の status が Done。

- [ ] **Step 5: タスククローズのコミット**

```bash
git add backlog/tasks/task-10-simple-webui.md
git commit -m "chore(backlog): TASK-10 WebUI を Done に

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review

**Spec coverage（設計 §2 スコープ対応）:**
- webui クレート/yew SPA/trunk 固定名出力 → Task 3。
- mube-core webapi（状態⇄JSON・ルート→状態）→ Task 1。serve.rs/command.rs 撤去 → Task 2。
- firmware picoserve・port 80・埋め込み・build.rs・TCP-6000 撤去・並行 worker → Task 4（互換スパイク）＋Task 5（本実装）。
- lockctl.ts fetch 移行（サブコマンド据え置き）→ Task 6。
- flake.nix に trunk/wasm32・束ねビルド → Task 3（trunk/wasm32）＋Task 5 Step 4（trunk→cargo の順序）。
- docs/README 更新 → Task 7。
- 非目標（認証/TLS/状態プッシュ/サーボ・WiFi 変更なし）→ 全タスクで触れない方針を明記。

**Placeholder scan:** picoserve の `asset`/`json`/listen API は「Task 4 で確定」と明示的に分離済み（放置プレースホルダではなく、依存の版依存性を潰す設計上のスパイク）。それ以外の Rust/TS/HTML は実コードを記載。

**Type consistency:** `LockState`/`Action`/`state_json`/`target_for` は Task 1 の定義と Task 5 の使用で一致。`parseState`/`formatReply`/`runLockctl(cmd, base, timeoutMs)` は Task 6 のテストと実装で一致。yew の `parse_state` は SPA 内ローカルで JSON 部分文字列判定（Task 3 内で完結）。

**既知リスク（設計 §9 と対応）:** 最大は「picoserve × embassy-net 0.9 の版整合」と「trunk × wasm-bindgen 版整合」。前者は Task 4 のスパイクで全配線前に確定し、整合版が無ければフォールバック判断へエスカレーション。後者は Task 3 Step 5 で devShell の wasm-bindgen 版に依存を合わせて解消する。
